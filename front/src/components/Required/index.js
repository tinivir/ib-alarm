import React from 'react';
import PropTypes from 'prop-types';

const Required = props => <span className="text-danger">*</span>;

Required.propTypes = {};
Required.defaultProps = {};

export default Required;
